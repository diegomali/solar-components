'use strict';

/**
 * Create a array based on a length, useful for arbitrary iterations.
 */
function count(to) {
  const array = [];
  for (let i = 0; i < to; i++) {
    array.push(i);
  }
  return array;
}

exports.count = count;

//# sourceMappingURL=count-65ba8c15.js.map