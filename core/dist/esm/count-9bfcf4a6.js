/**
 * Create a array based on a length, useful for arbitrary iterations.
 */
function count(to) {
  const array = [];
  for (let i = 0; i < to; i++) {
    array.push(i);
  }
  return array;
}

export { count as c };

//# sourceMappingURL=count-9bfcf4a6.js.map