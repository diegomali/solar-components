/**
 * A Form field validation that check if the field is filled.
 * @param errorMessage The custom error message. Commonly used for i18n.
 */
export declare function isRequired(errorMessage: any): (value: any) => string;
